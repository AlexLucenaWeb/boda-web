"use client";

import { useEffect, useRef } from "react";

export default function HeroImage() {

  return (
    <section className="relative max-w-full flex justify-center h-screen overflow-hidden">
        Alex

    </section>
  );
}
